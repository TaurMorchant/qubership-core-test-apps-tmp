name: Mesh-test-service-go-integration-tests build

on:
  push:
    paths:
      - 'mesh-test-service-go-integration-tests/**'
  pull_request:
    types: [ opened, synchronize, reopened ]
    paths:
      - 'mesh-test-service-go-integration-tests/**'

jobs:
  mvn:
    uses: netcracker/qubership-core-infra/.github/workflows/maven-build-with-sonar.yaml@fix/pom-file-path-is-ignored
    with:
      event-name: ${{ github.event_name }}
      actor: ${{ github.actor }}
      sonar-project-key: ${{ vars.SONAR_PROJECT_KEY }}
      upload-artifact: false
      pom-file-path: mesh-test-service-go-integration-tests/pom.xml
    secrets:
      sonar-token: ${{ secrets.SONAR_TOKEN }}